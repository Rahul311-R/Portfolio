# React Three Fiber 9.x declares react <19.3 as its peer range while this
# project uses react 19.3.x; the library is fully compatible in practice and
# the conflicting peers (expo/react-native) are optional and unused on web.
legacy-peer-deps=true
